var React = require('react');

var Detail = React.createClass({
    render: function () {
        return (
            <div>
                Detail
            </div>
        )
    }
});

module.exports = Detail;